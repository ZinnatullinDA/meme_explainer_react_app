{
  "name": "meme-explainer-backend",
  "version": "1.0.0",
  "private": true,
  "main": "src/server.js",
  "scripts": {
    "dev": "node --env-file=.env --watch src/server.js",
    "lint": "eslint src",
    "lint:ci": "eslint src --max-warnings=0",
    "lint:fix": "eslint src --fix",
    "start": "node --env-file=.env src/server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2"
  }
}
